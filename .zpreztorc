# Set case-sensitivity for completion, etc.
zstyle ':prezto:*:*' case-sensitive 'no'

# Color output
zstyle ':prezto:*:*' color 'yes'

# Set the Zsh modules to load (man zshmodules).
# zstyle ':prezto:load' zmodule 'attr' 'stat'

# Set the Zsh functions to load (man zshcontrib).
zstyle ':prezto:load' zfunction 'zmv' 'zcalc'

# Set the Prezto modules to load; the order matters.
# Some modules require git submodule init inside prezto.
#
# completion                Several command completions and options
# archive                   Provides `extract` and `ls-archive`
# command-not-found         Suggest packages to install.
# git                       Shows branch and icons; adds useless aliases.
# ssh-agent                 Allows using ssh-add automatically.
# syntax-highlighting       Live color syntax for commands.
# history-substring-search  Filters history for current command-line text.
# prompt                    Prompt themes.
#
zstyle ':prezto:load' pmodule \
  'completion' \
  'archive' \
  'command-not-found' \
  'git' \
  'ssh-agent' \
  'syntax-highlighting' \
  'history-substring-search' \
  'prompt'

#
# Prompt
#
# Set the prompt theme to load.
zstyle ':prezto:module:prompt' theme 'sorin'

#
# Syntax Highlighting
#
# Set syntax highlighters.
# By default main, brackets, and cursor are enabled.
zstyle ':prezto:module:syntax-highlighting' highlighters \
  'main' \
  'brackets'

# Load local configuration for different machines.
if [ -f ~/.zpreztorc.local ]; then
  source ~/.zpreztorc.local
fi
